# Sailfish zram — /etc/systemd/zram-generator.conf
# Compressed RAM swap: zstd at ~3:1 ratio gives effective memory headroom
# without touching disk. Critical when running LLMs + a game simultaneously.

[zram0]
zram-size = min(ram / 2, 8192)
compression-algorithm = zstd
swap-priority = 100
